# Reserved namespace for first-party framework integrations. Submodules
# (e.g. `saltare.contrib.django`) opt into their own dependency extras
# in `pyproject.toml` so the base wheel stays free of framework deps.
